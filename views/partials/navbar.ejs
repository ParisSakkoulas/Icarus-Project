<nav class="navbar sticky-top navbar-expand-lg navbar-dark shadow-sm p-3" style="background-color: #395B64;">
    <div class="container-fluid">
        <a class="navbar-brand nav-link" href="/main">
            <strong>I C A R U S </strong><i class="fa-solid fa-book-open"></i>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/main">Αρχική</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/subjects">Μαθήματα</a>
                </li>
                <% if(currentUser && currentUser.Role==='University_Student' ) {%>


                    <% if(currentStudent && currentStudent.StatementStatus==="not" ) {%>
                        <li class="nav-item">
                            <a class="nav-link" href="formStatement">Δήλωση μαθημάτων</a>
                        </li>
                        <% } %>

                            <% if(currentStudent.StatementStatus!="not" ) {%>
                                <li class="nav-item">
                                    <a class="nav-link" href="subjectsStatement">Δηλωμένα Μαθήματα</a>
                                </li>
                                <% } %>

                                    <%}%>
                                        <% if(currentUser) {%>
                                            <li class="nav-item">
                                                <a class="nav-link" href="/profile">Προφίλ</a>
                                            </li>
                                            <% } %>
            </ul>
        </div>

        <div class="navbar-nav">

            <!--Εμφάνιση των login, register σε περίπτση που δεν έχει συνδεθεί ο χρήστης-->
            <% if(!currentUser) {%>
                <a class="nav-link" href="/login">Σύνδεση</a>
                <a class="nav-link" href="/register">Εγγραφή</a>

                <!--Εμφάνιση μόνο της αποσύνδεσης σε περίπτωση που είναι συνδεδεμένος-->
                <% }else {%>
                    <a class="nav-link" href="/logout">Αποσύνδεση</a>
                    <% } %>
        </div>
    </div>
</nav>

<style>
    i:hover,
    strong:hover {
        color: #E7F6F2;
    }
</style>